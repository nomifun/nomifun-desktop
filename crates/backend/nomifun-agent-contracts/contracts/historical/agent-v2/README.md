# Historical Agent-v2 inputs (not current deletion instructions)

These six D-014 deletion manifests and the old composition inventory describe
the superseded external Codex Wrapper/Fresh-v4 replacement plan. Their source
content is retained for historical contract digests; references inside them are
references to that historical tree, not assertions about current reachable code.

In particular, do not delete ConversationService, the Nomi Engine, or shared
domain owners based on these manifests. CAR-D-019 and CAR-D-021 retain the
Conversation owner and support independent, compiled-in Nomi/Coding/community
Engines. Old passing-gate notes do not validate the current implementation.

The contract generator reads these archived inputs only to preserve the existing
deletion_manifest_set digest field. No current deletion or release gate should
interpret them as an executable work plan. The historical C1-C9/AP-7 gate CLI is
retired. Current ownership is described by contracts/inventory/current-composition.json;
current work and acceptance remain in docs/specs/2026-09-06-coding-agent-runtime-internalization/.
